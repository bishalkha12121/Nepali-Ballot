# Copy this file to .env.local for local development
# Then start your local backend server on port 8001

# For local backend (run backend first on port 8001):
REACT_APP_BACKEND_URL=http://localhost:8001

# For using the live cloud backend (no local backend needed):
# REACT_APP_BACKEND_URL=https://nepali-election-1.preview.emergentagent.com
